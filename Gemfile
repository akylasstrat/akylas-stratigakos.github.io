source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
# Optional extra plugins; many are bundled in github-pages
# gem "jekyll-scholar"

gem "jekyll-scholar", group: :jekyll_plugins
